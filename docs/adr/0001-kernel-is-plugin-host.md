# 内核是插件宿主，不是回合循环

对照 pi（循环即内核）与 DeepSeek Harness（连循环也是插件）之后，规格把**内核**定为进程内插件宿主：服务注册、依赖注入、生命周期、事件总线原语。回合循环是可整颗替换的**循环插件**；「删掉则无法完成模型 ↔ 工具回合」这条检验用在循环插件上，不用在内核上。发现、boot、业务事件名、模型、工具、会话都不进内核。

这样「一切皆插件」有真正的宿主可挂，同时不把 CLI/profile/回合合同焊进核。代价是核内没有 agent 语义——v0 能跑起来依赖默认循环插件的合同，须另锁。
