# 到 v0 产品闭环共六段，其后加槽不排序

实现路线图在到达自己用的 coding CLI 之前切成六段：宿主 → 默认循环插件 → `llm` 端口 → 流式 REPL → 默认工具包（四件套+搜索+ASK 一整包）→ MCP 工具桥。MCP 桥退出即 **v0 产品闭环**；之后才允许谈可嵌入 Runtime 与多智能体实现。会话/可观测/沙箱等后续阶段本图不排序，只锁不变量：不改 `loop`/`tools`/`llm` 语义、不加内核第五件套。不按每把工具拆阶段。
