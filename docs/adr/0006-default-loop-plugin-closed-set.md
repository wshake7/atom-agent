# 默认循环插件：三角消息 + 推理块 + 回合机械装置

默认循环占 `loop` 槽。闭合集：三种 role（`user` / `assistant` / `toolResult`）；`assistant` 上的推理块必须保留回放；工具分发（默认串行）、流式消费与 Abort 都在这颗插件内；只消费 `llm` 槽，不内置提供商。当前运行只有内存消息列表，不写盘。业务事件名由循环契约锁死并打到内核匿名总线（turn 开始/结束、助手流式、工具开始/结束），供 REPL 订阅。压缩、记忆、持久化不进这颗插件。
