# 日常 CLI 闭环的退出条件

Type: grilling
Label: wayfinder:grilling
Triage: ready-for-human
Status: resolved
Blocked by:

## Question

**日常 CLI 闭环**分成哪些可检验的阶段边界，每段必须有 / 明确没有 / 退出条件是什么？

已锁进本图的能力簇：长上下文、跨进程会话、装配与配置、Skill、REPL 手感、提供商兼容包（新包，不锁进 `llm`）。不要把范围外的簇（权限、TUI、plan/todo、并行批、Runtime、多智能体）写进退出条件。

至少裁定：

- 阶段名称与顺序（可以是一段，也可以按簇切开；不要按实现任务拆）
- 每段：必须有的、明确没有的、不得破坏的 v0 不变量
- 何谓本图目的地达成：可检验的退出，不是愿望清单
- 本阶段是否允许点名新官方槽——只锁「允许与否的阶段规则」，不在本票点出槽名

不写代码，不把阶段拆成工程任务列表。对照调研若已落地，用结论，不贴解剖。

## Answer

**日常 CLI 闭环**仍是一段阶段边界。走到它的路线图共四段；最后一段退出即目的地达成。六簇全部必须有。硬权衡见 [ADR-0009](../../../docs/adr/0009-roadmap-four-phases-to-daily-cli.md)。

槽规则（只锁规则，不点名）：允许为某段退出条件要求的能力点名新官方槽；禁止预埋空槽；不改 `loop` / `tools` / `llm` 语义；内核不加第五件套。具体名单见 [本阶段点哪些官方槽](./12-official-slots-this-stage.md)。

压缩与记忆是两件事：压缩必须有；记忆（跨会话旁路存储）明确没有，本图不设计记忆库。

| 阶段 | 必须有 | 明确没有 | 退出条件 |
| --- | --- | --- | --- |
| 装配与配置 | 用户级 + 项目级能改装配（至少：模型端点、MCP 列表、工具开关）；宿主仍只吃已解析的同进程模块 | 插件市场、远程配置中心、发现逻辑进内核 | 不改源码、只改配置，启动后的装配跟着变 |
| 跨进程会话 | 重启后能找回对话（落盘集与恢复面由后续票锁） | 多机同步、加密、记忆库 | 关掉进程再开，上一会话的消息还在 |
| 长上下文 | 压缩改变下一轮模型可见历史 | 记忆、压缩算法/阈值 | 长对话下一轮送给模型的是压缩后的视图；会话日志不因此只剩压缩稿 |
| Skill · 兼容包 · REPL 手感 | 能按需加载 Skill；新包接上至少一种提供商兼容面且方言不进 `llm` 槽；REPL 达到本图手感最小集 | Skill 市场、多提供商市场、差分 TUI、完整 readline 选型 | 三件事都能单独验过。此段退出 = **日常 CLI 闭环** |

全程不变量（写在规格路线图章首，不每行重复）：

1. 不改写 [v0 spec](../../pluggable-agent-spec/spec.md)
2. 不改 `loop` / `tools` / `llm` 语义
3. 内核不加第五件套
4. 不预埋空官方槽；新槽只为某段退出条件要求的能力点名
5. 提供商方言不锁进 `llm` 槽
6. 界面仍是流式 REPL，无差分 TUI
7. 无权限弹窗、无默认沙箱
8. 无可嵌入 Runtime、无多智能体
9. 无 npm / 目录插件市场

合同细节（Skill 加载面、压缩挂点、落盘集、配置格式、斜杠集合、兼容包边界）不在本票，分属后续票。

## Comments

- [装配与配置形态](./08-assembly-and-config-shape.md) **不**收窄不变量第 9 条。本地 `plugins/` 扫描与 hooks 都不进本图，留给后续图。`skills/` 搜索根仍由该票锁给 Skill 簇。发现仍不进内核。
